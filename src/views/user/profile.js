import React from 'react';
import {
    Text,
    View,
    SafeAreaView,
} from 'react-native';

export default function UserProfile () {
    return (
        <SafeAreaView>
            <View>
                <Text>User Profile</Text>
            </View>
        </SafeAreaView>
    );
};